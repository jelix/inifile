Some classes to read and modify ini files by preserving comments and empty lines.

They support sections and array values. You can also merge sections, merge two
ini files, and rename some values or sections.

# installation

You can install it from Composer. In your project:

```
composer require "jelix/inifile"
```

# Usage

See DOCUMENTATION.md.
